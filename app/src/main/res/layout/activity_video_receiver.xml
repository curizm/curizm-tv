<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0b0b11">

    <!-- Video Player -->
    <com.google.android.exoplayer2.ui.PlayerView
        android:id="@+id/videoPlayer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000000"
        android:layout_gravity="center"
        app:resize_mode="fit"
        app:surface_type="surface_view"
        app:show_buffering="when_playing"
        app:use_controller="false" />

    <!-- Black Overlay for transitions -->
    <View
        android:id="@+id/blackOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000000"
        android:alpha="0"
        android:visibility="gone" />

    <!-- Info Overlay -->
    <LinearLayout
        android:id="@+id/infoOverlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="24dp"
        android:orientation="vertical"
        android:background="@drawable/info_overlay_background"
        android:padding="24dp"
        android:minWidth="320dp"
        android:minHeight="100dp"
        android:alpha="0"
        android:visibility="gone">

        <TextView
            android:id="@+id/artworkTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Untitled Artwork"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/curizm_cream"
            android:layout_marginBottom="8dp"
            android:maxLines="2"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/artworkDetails"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No additional information"
            android:textSize="15sp"
            android:textColor="@color/curizm_cream_dark"
            android:maxLines="3"
            android:ellipsize="end" />

    </LinearLayout>

    <!-- Status Pills -->
    <LinearLayout
        android:id="@+id/statusPills"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:orientation="horizontal"
        android:alpha="0.6">

        <TextView
            android:id="@+id/roomPill"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Room: Loading..."
            android:textSize="12sp"
            android:textColor="@color/curizm_cream"
            android:background="@drawable/pill_background"
            android:padding="8dp"
            android:layout_marginEnd="8dp" />

        <TextView
            android:id="@+id/connectionPill"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="WS: connecting..."
            android:textSize="12sp"
            android:textColor="@color/curizm_cream"
            android:background="@drawable/pill_background"
            android:padding="8dp" />

    </LinearLayout>

</FrameLayout>
